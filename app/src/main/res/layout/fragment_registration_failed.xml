<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.RegistrationFailedFragment"
    android:background="@color/black">

    <ImageView
        android:id="@+id/registration_failed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/registration_failed">
    </ImageView>

    <ImageView
        android:id="@+id/close"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@+id/registration_failed"
        app:layout_constraintBottom_toBottomOf="@+id/registration_failed"
        android:layout_marginLeft="50dp"
        android:layout_marginBottom="30dp"
        android:src="@drawable/close">
    </ImageView>
    <ImageView
        android:id="@+id/check2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@+id/close"
        app:layout_constraintBottom_toBottomOf="@+id/close"
        android:layout_marginLeft="10dp"
        android:src="@drawable/check2">
    </ImageView>
</androidx.constraintlayout.widget.ConstraintLayout>